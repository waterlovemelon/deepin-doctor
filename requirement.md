# 需求文档

本文档是当前项目总体的需求文档，只保存需求不涉及UI和架构设计，后续持续更新迭代。

## 项目的目的

### 背景

在工作中（特别是中台业务），经常会遇到客户现场各种各样的问题，前场工作人员收集信息的时候会出现疏漏，或者需要和开发人员来回沟通需要哪些日志信息、设备信息、系统状态等。非常耽误时间，容易出现信息不对齐的情况，拉长了整个业务时长。

### 目的

做出一个工具，现场工作人员能够使用这个工具一键收集各种信息，导出给研发人员或者AI来分析，同时还能检查一些简单错误（环境被破坏、缺包漏包等）。这个工具需要具备一下特性：

1. 使用Qt框架进行开发。
2. 模块化架构，易扩展。可以提供接口给其它的业务部门开发插件，进行扩展。
3. 前后端分离，使用dbus通讯。

## 具体需求

### 获取设备的网络状态

中台经常遇到网络问题，例如网络无法连接、网络连接但是无法上网、网络自动断开连接、网络模块丢失、任务栏网络状态不正确等问题。本工具需要有一个网络模块，检测、抓取和处理一部分问题。

#### 网络检测项

- 网络连接状态（有线/无线）
- 网络配置信息（IP、网关、DNS）
- 网络连通性测试（ping网关、ping外网）
- DNS解析测试
- 网络服务状态（NetworkManager、wpa_supplicant等）
- 网络设备信息（网卡型号、驱动状态）

#### 日志收集

- NetworkManager日志
- wpa_supplicant日志
- 内核网络相关日志（dmesg | grep -i network）
- 系统日志中的网络错误

#### 常见问题检测

- DNS配置错误
- IP冲突检测
- 网卡驱动问题
- 防火墙规则异常
- 网络模块是否有丢失（任务栏/usr/lib/dde-dock/plugins和/usr/lib/dde-control-center/modules目录下面是否有网络插件）

### 系统信息收集模块

收集设备的基本信息，方便研发人员快速了解现场环境。

#### 硬件信息

- CPU型号、核心数、使用率
- 内存大小、使用率、交换分区
- 磁盘信息（分区、使用率、IO状态）
- 显卡信息
- 网卡信息

#### 系统信息

- 系统版本（lsb-release、os-release）
- 内核版本（uname -a）
- 桌面环境版本
- 关键组件版本（Qt、glibc、systemd等）
- 系统启动时间

#### 日志收集

- 系统日志（/var/log/syslog、journalctl）
- 内核日志（dmesg、/var/log/kern.log）
- X11/Wayland日志
- 桌面环境日志

### 环境检测模块

检测系统环境是否完整，是否有被破坏的迹象。

#### 依赖检查

- 核心依赖包完整性（dpkg/rpm验证）
- 关键库文件是否存在
- 环境变量配置检查（PATH、LD_LIBRARY_PATH等）

#### 配置检查

- 系统配置文件完整性
- 用户配置文件检查
- 服务配置检查

#### 常见问题检测

- 缺包/漏包检测
- 包管理器锁文件检测
- 系统更新中断检测
- 磁盘空间不足检测
- 文件权限异常检测

### 应用程序日志收集模块

收集中台业务相关的应用日志。

#### 日志收集范围

- 应用程序标准输出日志
- 应用程序错误日志
- 应用程序配置文件
- 应用程序运行状态

#### 日志处理

- 日志文件大小限制（超大日志截取）
- 日志时间范围筛选
- 敏感信息脱敏（密码、token等）

### 插件系统需求

允许其他业务部门开发插件，扩展功能。

#### 插件接口设计

- 插件需要实现的接口（信息收集、问题检测、日志抓取）
- 插件注册机制
- 插件生命周期管理（加载、初始化、运行、卸载）

#### 插件隔离

- 插件崩溃不影响主程序
- 插件执行超时机制
- 插件权限控制（文件访问、网络访问等）

#### 插件通信

- 通过DBus与主程序通信
- 支持异步调用
- 支持进度反馈

### 导出功能需求

将收集的信息导出，方便传输和分析。

#### 导出格式

- 压缩包格式（tar.gz/zip）
- 目录结构清晰（按模块分类）
- 包含清单文件（JSON格式，记录收集时间、模块列表等）

#### 导出内容组织

```
export_YYYYMMDD_HHMMSS/
├── manifest.json          # 收集清单
├── system_info.json       # 系统信息
├── network/               # 网络模块
│   ├── network_info.json
│   └── logs/
├── logs/                  # 系统日志
│   ├── syslog
│   ├── kern.log
│   └── dmesg
└── plugins/               # 插件收集的信息
    └── plugin_name/
```

### 用户交互需求

#### 基本原则

- 界面简洁直观
- 一键收集/导出
- 进度可视化
- 错误提示友好

#### 功能入口

- 主界面：模块选择（全选/单选）
- 一键诊断按钮
- 一键收集按钮
- 导出按钮
- 历史记录查看

#### 结果展示

- 收集进度实时显示
- 检测到的问题列表
- 问题严重程度标识（错误/警告/信息）
- 一键复制问题信息

### 非功能性需求

#### 性能要求

- 启动时间 < 3秒
- 日志收集不占用过多资源（CPU < 20%, 内存 < 200MB）
- 大日志文件处理优化（分块读取）

#### 稳定性要求

- 单个模块崩溃不影响其他模块
- 超时机制（单个操作最多等待30秒）
- 异常捕获和日志记录

#### 安全性要求

- 不收集敏感信息（密码、证书等）
- 日志文件权限控制（仅当前用户可读）
- 导出文件可选择加密

